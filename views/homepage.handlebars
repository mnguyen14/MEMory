{{#each users as |user| }}
<div class="row align-center mb-5">
    <div class="col-md-6">
        <h2>{{user.name}}</h2>
    </div>
    <div class="col-md-6">
        <h3>MEMory is a new way to journal and keep track of your mental health</h3>
    </div>
</div>